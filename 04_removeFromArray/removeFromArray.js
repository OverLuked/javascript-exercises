const removeFromArray = function(array, index) {
    let newArray = [];

    for (let i = 0; i < arr.length; i++) {
        newArray[i] = (arr.i !== index) ? arr[i]: null;
    }
    
    return newArray;
};

// Do not edit below this line
module.exports = removeFromArray;
